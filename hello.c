#include <stdio.h>

/**
 * main - The entry point for the C program.
 * return: Always returns 0 upon successful execution.
 */
int main(void) {
    // printf() sends a welcome message and "Hello, World!" to standard output (stdout).
    printf("Welcome to the C programming world! Hello, World!\n");
    return 0;
}